import { settings } from './Settings';

const { iotPrefix } = settings;

export const FILTER_EMPTY_STRING = `${iotPrefix}-FILTER_EMPTY_STRING`;
export const EMPTY_STRING_DISPLAY_VALUE = 'Unspecified';
